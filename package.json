{
  "name": "arc-hsc-maths",
  "version": "0.1.0",
  "private": true,
  "description": "Arc — a browser-based NSW HSC mathematics practice app.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^0.20.5"
  }
}
